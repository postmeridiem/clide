// Based on xterm.dart v4.0.0 by xuty (MIT). See LICENSE in this directory.

// The enum lives in core so the terminal model can hold the shape a program
// asks for with DECSCUSR (T-397) without core importing ui.
export 'package:clide/src/terminal/src/core/cursor.dart' show TerminalCursorType;
